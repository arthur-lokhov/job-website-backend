services:
  db:
    ports:
      - "${DB_PORT:-5432}:5432"
  cache:
    ports:
      - "${VALKEY_PORT:-6379}:6379"
  app:
    ports:
      - "${HTTP_SERVER_PORT:-8080}:8080"
    build:
      context: .
      dockerfile: Dockerfile

